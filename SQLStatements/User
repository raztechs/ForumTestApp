# Create the user database table
# Table will have id, username and password fields

CREATE TABLE user
(
	user_ID int NOT NULL,
	username varchar(255),
	password varchar(255),
	PRIMARY KEY (user_ID)
);
